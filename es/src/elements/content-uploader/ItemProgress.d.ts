import * as React from 'react';
import './ItemProgress.scss';
export interface ItemProgressProps {
    progress: number;
}
declare const ItemProgress: ({ progress }: ItemProgressProps) => React.JSX.Element;
export default ItemProgress;
