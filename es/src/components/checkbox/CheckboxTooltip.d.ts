import * as React from 'react';
export interface CheckboxTooltipProps {
    tooltip: string;
}
declare const CheckboxTooltip: ({ tooltip }: CheckboxTooltipProps) => React.JSX.Element;
export default CheckboxTooltip;
