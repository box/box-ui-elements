import * as React from 'react';
import { ButtonProps } from '../button/Button';
declare const PrimaryButton: ({ children, className, ...rest }: Partial<ButtonProps>) => React.JSX.Element;
export default PrimaryButton;
