import * as React from 'react';
import { Icon } from '../iconTypes';
declare const NoNotificationState: ({ className, color, height, opacity, title, width, }: Icon) => React.JSX.Element;
export default NoNotificationState;
