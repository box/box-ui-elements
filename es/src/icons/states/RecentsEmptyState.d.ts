import * as React from 'react';
import { Icon } from '../iconTypes';
declare const RecentsEmptyState: ({ className, color, height, title, width }: Icon) => React.JSX.Element;
export default RecentsEmptyState;
