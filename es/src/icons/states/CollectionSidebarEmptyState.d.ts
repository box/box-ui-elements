import * as React from 'react';
import { Icon } from '../iconTypes';
declare const CollectionSidebarEmptyState: ({ className, color, height, title, width }: Icon) => React.JSX.Element;
export default CollectionSidebarEmptyState;
