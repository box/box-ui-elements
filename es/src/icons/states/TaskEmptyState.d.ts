import * as React from 'react';
import { Icon } from '../iconTypes';
declare const TaskEmptyState: ({ className, color, height, title, width }: Icon) => React.JSX.Element;
export default TaskEmptyState;
