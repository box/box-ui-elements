import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconFolderPersonal: ({ className, height, title, width }: Icon) => React.JSX.Element;
export default IconFolderPersonal;
