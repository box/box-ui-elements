import * as React from 'react';
import { FileIcon } from '../iconTypes';
declare const OfficeOnlineIcon: ({ className, dimension, extension, title }: FileIcon) => React.JSX.Element;
export default OfficeOnlineIcon;
