import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconWatermark: ({ className, color, height, title, width }: Icon) => React.JSX.Element;
export default IconWatermark;
