import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconUploadCloud: ({ className, color, height, title, width }: Icon) => React.JSX.Element;
export default IconUploadCloud;
