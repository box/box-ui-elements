import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconHamburger: ({ className, color, height, title, width }: Icon) => React.JSX.Element;
export default IconHamburger;
