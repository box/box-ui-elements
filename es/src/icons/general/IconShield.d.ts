import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconShield: ({ className, color, height, opacity, width, title }: Icon) => React.JSX.Element;
export default IconShield;
