import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconBox3DCenter: ({ className, height, color, title, width }: Icon) => React.JSX.Element;
export default IconBox3DCenter;
