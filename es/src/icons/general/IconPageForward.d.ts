import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconPageForward: ({ className, color, height, title, width }: Icon) => React.JSX.Element;
export default IconPageForward;
