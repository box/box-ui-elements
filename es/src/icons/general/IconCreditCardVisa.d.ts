import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconCreditCardVisa: ({ className, color, height, opacity, title, width, }: Icon) => React.JSX.Element;
export default IconCreditCardVisa;
