import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconAutomation: ({ className, color, height, title, width }: Icon) => React.JSX.Element;
export default IconAutomation;
