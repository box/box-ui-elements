import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconAddMetadataEmptyState: ({ className, color, title, width }: Icon) => React.JSX.Element;
export default IconAddMetadataEmptyState;
