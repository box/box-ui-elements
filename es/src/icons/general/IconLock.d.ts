import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconLock: ({ className, color, height, opacity, title, width }: Icon) => React.JSX.Element;
export default IconLock;
