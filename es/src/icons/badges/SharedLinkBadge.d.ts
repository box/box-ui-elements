import * as React from 'react';
import { Icon } from '../iconTypes';
declare const SharedLinkBadge: ({ className, color, height, title, width }: Icon) => React.JSX.Element;
export default SharedLinkBadge;
