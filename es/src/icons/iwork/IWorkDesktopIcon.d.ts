import * as React from 'react';
import { FileIcon } from '../iconTypes';
declare const IWorkDesktopIcon: ({ className, dimension, extension, title }: FileIcon) => React.JSX.Element;
export default IWorkDesktopIcon;
