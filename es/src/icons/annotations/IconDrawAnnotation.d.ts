import * as React from 'react';
import { Icon } from '../iconTypes';
declare const IconDrawAnnotationMode: ({ className, color, height, title, width }: Icon) => React.JSX.Element;
export default IconDrawAnnotationMode;
