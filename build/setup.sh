#!/bin/bash

setup() {
    echo "----------------------------------------------"
    echo "Starting install, clean and locale build"
    echo "----------------------------------------------"
    if yarn setup; then
        echo "----------------------------------------------------"
        echo "Setup complete"
        echo "----------------------------------------------------"
    else
        echo "----------------------------------------------------"
        echo "Failed to setup!"
        echo "----------------------------------------------------"
        exit 1;
    fi
}
