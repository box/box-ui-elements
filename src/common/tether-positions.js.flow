// @flow
const TOP_LEFT: 'top left' = 'top left';
const TOP_CENTER: 'top center' = 'top center';
const TOP_RIGHT: 'top right' = 'top right';
const MIDDLE_LEFT: 'middle left' = 'middle left';
const MIDDLE_CENTER: 'middle center' = 'middle center';
const MIDDLE_RIGHT: 'middle right' = 'middle right';
const BOTTOM_LEFT: 'bottom left' = 'bottom left';
const BOTTOM_CENTER: 'bottom center' = 'bottom center';
const BOTTOM_RIGHT: 'bottom right' = 'bottom right';

const TETHER_POSITIONS = {
    TOP_LEFT,
    TOP_CENTER,
    TOP_RIGHT,
    MIDDLE_LEFT,
    MIDDLE_CENTER,
    MIDDLE_RIGHT,
    BOTTOM_LEFT,
    BOTTOM_CENTER,
    BOTTOM_RIGHT,
};

export type TetherPosition = $Values<typeof TETHER_POSITIONS>;

export default TETHER_POSITIONS;
