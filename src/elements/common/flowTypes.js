// @flow

type ErrorType = {
    code: string,
    details?: Object,
    displayMessage?: string,
    message?: string,
};

// eslint-disable-next-line import/prefer-default-export
export type { ErrorType };
