// @flow

export type MediaFeatures = {
    anyHover?: 'none' | 'hover',
    anyPointer?: 'none' | 'coarse' | 'fine',
    hover?: 'none' | 'hover',
    maxHeight?: number,
    maxWidth?: number,
    minHeight?: number,
    minWidth?: number,
    pointer?: 'none' | 'coarse' | 'fine',
};
