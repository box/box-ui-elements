`import Media from 'box-ui-elements/es/components/media/Media';`

> **Note**
>
> `Media` is a compound component that implements the "media object" element. The inner elements should not be used outside of the Media container.
