`import InlineNotice from 'box-ui-elements/es/components/inline-notice';`

The `InlineNotice` is an inline banner with customizable types: `warning`, `error`, `success`, `info`, or `generic`. It allows for an optional header.
