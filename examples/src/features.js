// feature config for feature flags
export default {
    activityFeed: {
        tasks: {
            createFromComment: true,
            createButton: true,
            newApi: true,
            newCards: true,
        },
    },
};
