// feature config for feature flags
export default {
    activityFeed: {
        tasks: {
            avatars: true,
            createFromComment: true,
            createButton: true,
        },
    },
};
